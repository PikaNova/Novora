import { useState } from 'react';
import { Ellipsis, LogIn, MonitorCog, Play } from 'lucide-react';
import '../styles/exam-quick-menu.css';
export default function ExamQuickMenu({onLocal,onAdmin,onTemporary}:{onLocal:()=>void;onAdmin:()=>void;onTemporary:()=>void}){const [open,setOpen]=useState(false);const go=(fn:()=>void,confirm?:string)=>{if(!confirm||window.confirm(confirm)){setOpen(false);fn()}};return <div className="exam-quick"><button aria-label="更多" title="更多" aria-expanded={open} onClick={()=>setOpen(v=>!v)}><Ellipsis /></button>{open&&<div className="exam-quick__menu"><button onClick={()=>go(onTemporary)}><Play />快速开始考试</button><button onClick={()=>go(onLocal)}><MonitorCog />本地设置</button><button onClick={()=>go(onAdmin,'进入管理后台？大屏将停止全屏展示。')}><LogIn />进入管理后台</button></div>}</div>}
